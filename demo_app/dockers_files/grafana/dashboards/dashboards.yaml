apiVersion: 1

providers:
  - name: 'json file based provider'
    # <int> Org id. Default to 1
    orgId: 1
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/jsons/